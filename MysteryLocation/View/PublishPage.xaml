<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MysteryLocation.View.PublishPage"
             NavigationPage.HasNavigationBar="False"
             NavigationPage.BackButtonTitle="False"
             ControlTemplate="{StaticResource Template}">
    <ContentPage.Content>
        <StackLayout>

            <Label x:Name="currentGPS"
               Text="{Binding CurrentPosition}"
               VerticalOptions="StartAndExpand"
               HorizontalOptions="Center"/>

            <Entry x:Name="entrySubject" Placeholder="Write subject here (obligatory)"/>

            <Entry x:Name="entryBody" Placeholder="Write appropriate text here (voluntary)"/>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Image x:Name="imgCam" Grid.Row="0" Grid.Column="0"/>
                
                

                <Button x:Name="btnCam" Text="SNAP A PIC" Grid.Row="1" Grid.Column="0" Margin="2" Clicked="BtnCam_Clicked"
BackgroundColor="#00a4fc" TextColor="#FFF"/>
                
            </Grid>
            <Button x:Name="btnPublish" Text="Publish post!" Grid.Row="1" Grid.Column="0" Margin="2" Clicked="PublishButton_Clicked"
BackgroundColor="#00a4fc" TextColor="#FFF"/>



            <ActivityIndicator x:Name="defaultActivityIndicator"
                               IsRunning="False" 
                               Color="CornflowerBlue"
                               VerticalOptions="CenterAndExpand"
                               HorizontalOptions="FillAndExpand" 
                               HeightRequest="80" 
                               WidthRequest="80"
                               />

        </StackLayout>
    </ContentPage.Content>
</ContentPage>